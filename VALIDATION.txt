D.S.F.S.T validation (2026-09-23)
=================================

Passed on the Windows development host:
- Python syntax compilation for the project services and database scripts.
- Bash syntax checks for install_dsfst.sh and start_dsfst.sh.
- Frontend TypeScript check and production build.
- Fifteen mocked smoke tests for parameter validation, local Origin protection,
  successful and failed stops, natural completion, network rule ownership,
  measured throughput, generated credentials, and Compose YAML configuration.
- The built frontend started and returned HTTP 200 on localhost:3000.
- A WSL smoke test exercised Linux file locking with tc and sudo mocked.

Passed in a fresh Ubuntu 24.04 Oracle VirtualBox guest:
- install_dsfst.sh installed the dependencies and pulled database images.
- start_dsfst.sh started MongoDB, InfluxDB, Redis, all eleven APIs, the
  dashboard, and the metrics writer.
- The dashboard, API health endpoints, experiment list, latest metrics,
  aggregate report, and orchestrator state returned HTTP 200.
- Live CPU, memory, latency, and packet-loss injections started and stopped
  through the orchestrator. Experiment details and reports returned HTTP 200.
- A short CPU injection completed naturally and its database record became
  completed without a manual stop after the metrics-writer reconciliation fix.

The tests in tests/ never inject faults into the host. The VirtualBox smoke
run used a dedicated guest and low-intensity, short-lived injections.

Windows VirtualBox clone launcher (2026-09-24):
- create_dsfst_vms.bat completed a dry run and created two full clones from
  the prepared LinuxVm1 snapshot, with independent disks, NAT adapters,
  4 GB RAM, and one CPU each. A repeat run reused existing managed VMs.
- DSFST-User-001 booted without a guest password prompt. The clone first-boot
  service removed its copied database volumes, generated new credentials,
  and started the app. The dashboard, /status, and /state returned HTTP 200.
- LinuxVm1 restarted from the same setup. Its clone reset was skipped and
  its existing data remained in place. Its dashboard and checked APIs
  returned HTTP 200 after startup.
- PowerShell parser and Bash syntax checks passed for the new scripts.

Database/API probe smoke (2026-09-24 UTC):
- Seventeen local smoke tests passed, including GET/POST handling and API path
  validation for dsfst_probe.py.
- In DSFST-User-001, `dsfst_probe.py status` returned HTTP 200 from the
  monitor, metrics API, and orchestrator after service startup.
- Direct connections to MongoDB, InfluxDB, and Redis succeeded. The InfluxDB
  query returned recent CPU, memory, and network fields; Redis was idle.
- A POST with an empty JSON body reached /experiments and returned HTTP 422 as
  expected. It created no experiment and started no injection.
- DSFST-User-001 was shut down cleanly after the check.
